---
- hosts: localhost
  connection: local
  become: true
  tasks:
  - name: Install htop
    apt:
      name: htop
  - name: Install tcpstat for tcp listening
    apt:
      name: tcpstat
   - name: Install Apache for webserver
    apt:
      name: apache2

